# from hashlib import sha256
import secrets


# def hash(message: str) -> str:
#     return sha256(message.encode()).hexdigest()


# def generate_salt(blen: int) -> str:
#     return str.from_bytes(secrets.token_bytes(blen), blen)

# print(generate_salt(16))
